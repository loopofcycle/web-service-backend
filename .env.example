# Copy to .env and fill in real values. Never commit .env.

APP_PORT=5000
FLOWER_PORT=5555

POSTGRES_USER=admin
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=db_pg
POSTGRES_HOST=db
POSTGRES_PORT=5432

ADMIN_USER=CHANGE_ME
ADMIN_PASSWORD=CHANGE_ME

REDIS_HOST=redis
REDIS_PORT=6379
BROKER_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://host.docker.internal:6379/0
REDIS_URL=redis://redis:6379/0

FRONTEND_HOST=http://localhost
CORS_ORIGINS=http://localhost,http://localhost:80,http://127.0.0.1

PUBLIC_API_BASE_URL=http://localhost:5000
INTERNAL_API_BASE_URL=http://localhost:5000

HOST_STORAGE_PATH=C:/Users/loopo/YandexDisk/_revit_library
SERVER_STORAGE_PATH=C:\Users\loopo\YandexDisk\_revit_library
MOUNTED_STORAGE_PATH=/data/rvt_files

# Schema migrations:
#   alembic upgrade head          # fresh / apply
#   alembic stamp head            # existing DB already created via init_db
# (uses DATABASE_URL or POSTGRES_* from this file)
